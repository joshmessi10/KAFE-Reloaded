import machine;

-- Interleaved points (bad clustering)
List[List[FLOAT]] X = [[0.0, 0.0], [10.0, 10.0], [1.0, 1.0], [11.0, 11.0]];
List[INT] labels = [0, 1, 1, 0];
FLOAT score = machine.silhouette_score(X, labels);
show(score);
