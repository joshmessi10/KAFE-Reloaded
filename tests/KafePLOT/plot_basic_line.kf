import plot;

-- Define the list of Y values:
List[INT] values = [10, 20, 15, 25, 60];

-- Since only Y values are available, create X = [0, 1, 2, 3, 4]:
List[INT] indexes = [0, 1, 2, 3, 4];

-- Now call correctly a plot.graph(X, Y):
plot.graph(indexes, values);
