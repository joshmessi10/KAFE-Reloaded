import plot;

plot.title("Library preferences");
plot.ylabel("Number of users");
plot.barValues(True);

List[STR] names = ["Matplotlib", "Seaborn", "Plotly", "Plotnine"];
List[INT] votes   = [2500, 1800, 3000, 2200];

plot.bar(names, votes);
