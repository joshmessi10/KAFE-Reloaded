drip get_first_even_number() => INT:
    List[INT] numbers = [1, 3, 5, 6, 7];
    for (n in numbers):
        if (n % 2 == 0):
            return (n);
        ;
    ;
;

show(get_first_even_number());
