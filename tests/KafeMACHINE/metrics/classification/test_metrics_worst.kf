import machine;

List[INT] y_true = [0, 0, 0];
List[INT] y_pred = [1, 1, 1];
FLOAT prec = machine.precision_score(y_true, y_pred);
FLOAT rec = machine.recall_score(y_true, y_pred);
FLOAT f1 = machine.f1_score(y_true, y_pred);
show(prec);
show(rec);
show(f1);
