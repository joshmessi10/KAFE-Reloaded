import machine;

-- Perfect multicollinearity → singular matrix
List[List[FLOAT]] X = [[1.0, 1.0], [2.0, 2.0], [3.0, 3.0]];
List[FLOAT] y = [1.0, 2.0, 3.0];

MACHINE lr = machine.linear_regression();
lr.fit(X, y);
