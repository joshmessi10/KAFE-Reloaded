import machine;

List[FLOAT] X = [1.0, 2.0, 3.0, 8.0, 9.0, 10.0];
List[INT] y = [0, 0, 0, 1, 1, 1];

MACHINE nb = machine.gaussian_nb();
nb.fit(X, y);

List[INT] preds = nb.predict([2.0, 9.0]);
show(preds);
