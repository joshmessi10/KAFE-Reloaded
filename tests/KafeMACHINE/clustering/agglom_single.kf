import machine;

List[List[FLOAT]] X = [[1.0, 1.0], [1.5, 1.5], [5.0, 5.0], [5.5, 5.5]];
MACHINE agg = machine.agglomerative_clustering(2, "single");
List[INT] labels = agg.fit_predict(X);
show(labels);
