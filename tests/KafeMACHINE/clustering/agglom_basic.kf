import machine;

List[List[FLOAT]] X = [[1.0, 1.0], [1.5, 1.5], [3.0, 3.0], [3.5, 3.5]];
MACHINE agg = machine.agglomerative_clustering(2, "ward");
List[INT] labels = agg.fit_predict(X);
show(labels);
