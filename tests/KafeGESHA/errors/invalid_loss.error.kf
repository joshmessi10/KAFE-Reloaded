import geshaDeep;

-- Test invalid loss function
GESHA model = geshaDeep.binary();
GESHA layer = geshaDeep.create_dense(1, "sigmoid", [2], 0.0, 42);
model.add(layer);

-- Try to compile with invalid loss
model.compile("sgd", "invalid_loss", []);
