import geshaDeep;

-- Test set_lr before compile
GESHA model = geshaDeep.binary();
GESHA layer = geshaDeep.create_dense(1, "sigmoid", [2], 0.0, 42);
model.add(layer);

-- Try to set learning rate before compile
geshaDeep.set_lr(model, 0.1);
