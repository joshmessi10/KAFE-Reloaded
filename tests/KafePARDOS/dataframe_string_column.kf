import pardos;

PARDOS df = pardos.read_csv("dataframe_string_column.csv");

PARDOS all = df;
show(all);

show(df.shape());

List[STR] cities = df.col("city");
show(cities);

show(df.dtypes());

show(df.info());

PARDOS desc = df.describe();
show(desc);
