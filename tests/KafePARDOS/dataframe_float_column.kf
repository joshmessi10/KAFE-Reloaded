import pardos;

PARDOS df = pardos.read_csv("dataframe_float_column.csv");

PARDOS all = df;
show(all);

show(df.shape());

List[FLOAT] values = df.col("value");
show(values);

show(df.dtypes());

show(df.info());

PARDOS desc = df.describe();
show(desc);
