import pardos;

-- Test bfill() method (backward fill)
PARDOS df = pardos.read_csv("column_manipulation_test.csv");

show("Original DataFrame:");
show(df);

-- Backward fill NaN values
PARDOS df2 = df.bfill();
show("After backward fill:");
show(df2);
