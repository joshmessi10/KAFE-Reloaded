def raiseDifferentDimension(function):
    raise Exception(f"{function}: Matrices don't have the same dimension")

def raiseNonUniformMatrix(function):
    raise Exception(f"{function}: Matrix is not uniform")
